


// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int a = '0';
    printf("%d, ", sizeof(++a));
    printf("%d", a);
    
    return 0;
}


// Online Javascript Editor for free
// Write, Edit and Run your Javascript code using JS Online Compiler
    
let errors = 0;
function check(output){
    if(!output)
        errors +=1;
}
check(true);
check(1);
check(0);
console.log(errors);

// Online C compiler to run C program online
#include <stdio.h>

void main() {
    // Write C code here
    int x = 4, z = 3;
    int y = (x << 3) + (~z);
    printf(" %d\n", y);
}


// Online Javascript Editor for free
// Write, Edit and Run your Javascript code using JS Online Compiler


function hacker(){
    let count = 0;
    if(count === 1){
        console.log(count++);
    }
    else {
        count--;
    }
}
hacker();
hacker();
hacker();
console.log(count);
## ERROR





#include<stdbool.h>
#include<malloc.h>

int Solve (int N, int* A) {
   // Write your code here
}

int main() {
    int N;
    scanf("%d", &N);
    int *A = (int *)malloc(N*sizeof(int));
    for (int i = 0; i < N; ++i) {
        scanf("%d", &A[i]);
    }
    int out_ = Solve(N, A);
    printf("%d\n", out_);
}






Google Online Challenge for TSS 2022
Difficulty Level : Hard
Last Updated : 10 Nov, 2021
For the position Technical Solution Specialist. Test was conducted on Cocubes.

Time : 30 Minutes

No. of Question : 28

Topics: Web Technologies (5 Questions)

Networking (5 Questions)
Linux/Unix (5 Questions)
Database (5 Questions)
Code Fundamentals (8 Questions)

Networking :

DNS Port Number : 53
CDN Full form : Content Delivery Network
ECMP Protocol : equal cost multi path protocol
TCP and UDP differences
Loss % MTR
Web Technologies:

Center a paragraph using multiple web teck : html,css,javascript,jquery
How a web server handle multiple request :
Content service by CDN which is not useful
HTTP Status code for invalid credentials : 401
HTTP Method to reterive information of resource : GET
UNIX/LINUX:

What is Load Average : avg system load
Permissions to open a file,modify and save : read and write
Analysis on output of free -m
How to execute command as another user : sudo
& operator is used for : to run command in background
Code Fundamentals:

Worst Case complexity of Quick Sort :
Number of nodes at 3rd level of fully binary tree :
Data Structure for FIFO Tickets distribution : Queue
Code
Code
How to save password :
Binary search is based on : Divide and Conquer
How to detect loop in a linked list in optimal way : two pointer approach
Database:

Outer join
SQL query to count records
Transaction serial
big table query optimaztion
To insert 2 row in a table but snapshot isolation is applied what will happen